---
quick_id: 260504-tci
slug: add-multiply-function
description: Add a multiply(a, b) function to hello.js that returns a * b, with a simple test
date: 2026-05-04
status: planned
---

# Quick Task: Add multiply(a, b) to hello.js

## Goal

Add a `multiply(a, b)` function to `hello.js` that returns `a * b`, and add a corresponding test.

## Must Haves

- [ ] `multiply(a, b)` function exported from `hello.js`
- [ ] Function returns `a * b`
- [ ] Test file (or test in existing test file) that covers `multiply`
- [ ] `npm test` passes

## Tasks

1. Add `multiply(a, b)` function to `hello.js` and export it
2. Create `hello.test.js` with a simple test for `multiply`
3. Run `npm test` to verify

## Files

- `hello.js` — add function + export
- `hello.test.js` — new test file
